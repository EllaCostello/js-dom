


// Intro DOM Exercise
// ==================
// 1. Read the text content of #message and log it to the console as soon as the page loads (no event handlers).
// 2. Also, log the text of all elements with class .msg, one per line.
// 3. Use textContent to read the messages.

window.addEventListener("load", initApp);

function initApp() {
    // TODO: find the #message text and log it to the console
    // TODO: Find all .msg texts, and log each line to the console
}
